Python
pip install needed
- fastapi: backend web server that receives requests from the frontend and decides what to do.
- uvicorn: he program that runs your FastAPI application and listens for incoming HTTP requests.
- SQLAlchemy: a popular Python library that lets you talk to databases (like PostgreSQL, MySQL, or SQLite) using Python code instead of raw SQL
- Psycopg: Python driver that allows Python programs to communicate with PostgreSQL